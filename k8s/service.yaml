apiVersion: v1
kind: Service

metadata:
  name: agentops-service
  labels:
    app: agentops

spec:
  type: NodePort

  selector:
    app: agentops

  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080